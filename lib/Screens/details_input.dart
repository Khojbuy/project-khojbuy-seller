import 'package:flutter/material.dart';

class DetailsInput extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(
        body: SingleChildScrollView(
          child: Center(child: Text("Taking the user details!")),
        ),
      ),
    );
  }
}
